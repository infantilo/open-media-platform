{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://openmediaplatform.local/schemas/descriptor-v0.schema.json",
  "title": "OMP Descriptor v0",
  "description": "Selbstbeschreibung eines Nodes: Parameter und Methoden. Flaches, bewusst einfaches Format (ARCHITECTURE.md §8 Fallback-Klausel) mit einer Mapping-Notiz nach IS-12/14 (MS-05-02 Control Framework) in docs/decisions.md — Ziel ist eine spätere Migration, sobald IS-12/14-Tooling ausgereift ist, ohne dass sich das grundlegende Konzept (selbstbeschreibende Parameter/Methoden statt Orchestrator-Hardcoding) ändert.",
  "type": "object",
  "required": ["parameters", "methods"],
  "properties": {
    "parameters": {
      "type": "array",
      "items": { "$ref": "#/$defs/parameter" }
    },
    "methods": {
      "type": "array",
      "items": { "$ref": "#/$defs/method" }
    },
    "latency": {
      "description": "Optionale Per-Node-Latenzdeklaration (ARCHITECTURE.md §15, UMSETZUNG.md D8 Teil 1) — additiv, ein Node ohne dieses Feld bleibt für den künftigen Budget-Rechner \"Latenz unbekannt\" (§15.2), sonst unverändert lauffähig. Fehlt oder null, wenn der Node keine Angabe macht.",
      "oneOf": [
        { "type": "null" },
        { "$ref": "#/$defs/latencyInfo" }
      ]
    }
  },
  "additionalProperties": false,
  "$defs": {
    "parameter": {
      "type": "object",
      "description": "Ein Parameter, den ein Node über GET/PATCH /params/<name> lesbar bzw. (falls nicht readonly) schreibbar macht.",
      "required": ["name", "type", "readonly"],
      "properties": {
        "name": { "type": "string" },
        "type": {
          "type": "string",
          "enum": ["number", "boolean", "enum", "string"]
        },
        "unit": {
          "description": "Physikalische Einheit (z. B. \"dB\"), nur für numerische Parameter relevant. Fehlt oder null, wenn nicht anwendbar.",
          "type": ["string", "null"]
        },
        "range": {
          "description": "Wertebereich. Für type=number: min/max. Für type=enum: erlaubte values. Fehlt oder null, wenn nicht anwendbar (z. B. type=string, type=boolean).",
          "oneOf": [
            { "type": "null" },
            {
              "type": "object",
              "required": ["min", "max"],
              "properties": {
                "min": { "type": "number" },
                "max": { "type": "number" }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": ["values"],
              "properties": {
                "values": {
                  "type": "array",
                  "items": { "type": "string" }
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "readonly": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "method": {
      "type": "object",
      "description": "Eine Methode, die ein Node über POST /methods/<name> ausführt.",
      "required": ["name", "args"],
      "properties": {
        "name": { "type": "string" },
        "args": {
          "type": "array",
          "items": { "$ref": "#/$defs/methodArg" }
        }
      },
      "additionalProperties": false
    },
    "methodArg": {
      "type": "object",
      "required": ["name", "type"],
      "properties": {
        "name": { "type": "string" },
        "type": {
          "type": "string",
          "enum": ["number", "boolean", "enum", "string"]
        }
      },
      "additionalProperties": false
    },
    "latencyInfo": {
      "type": "object",
      "description": "Inhärente Verarbeitungslatenz eines Nodes, getrennt nach Medienart (§15.1 Punkt 5: ein Video-Frame-Budget ist kein automatisches Audio-/Daten-Budget). video/audio/data sind jeweils in der Grain-Rate des betroffenen Flows zu verstehen (z. B. Video-Frames bei Videoraten, Samples bei Audio).",
      "properties": {
        "video": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/latencyRange" }] },
        "audio": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/latencyRange" }] },
        "data": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/latencyRange" }] },
        "supportsDelayCompensation": {
          "description": "true, falls der Node zusätzlich zu seiner inhärenten Latenz per POST /methods/setOutputDelay {\"frames\": N} eine einstellbare Zusatzverzögerung unterstützt (§15.1 Punkt 3, D8 Teil 3). false/fehlend = kein Delay-Ausgleich möglich.",
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "latencyRange": {
      "type": "object",
      "required": ["minLatencyFrames", "maxLatencyFrames"],
      "properties": {
        "minLatencyFrames": { "type": "integer", "minimum": 0 },
        "maxLatencyFrames": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    }
  }
}
