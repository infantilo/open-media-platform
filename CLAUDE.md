# OpenMediaPlatform

Neues, eigenständiges Projekt. Kein Code- oder Git-Zusammenhang mit
`/home/infantilo/PIPELINE CONTROLLER` — separates Repo, separates Memory.

## Bezug zu PIPELINE CONTROLLER

Für Broadcast-/GStreamer-/Playout-Know-how (gst-kit-Patterns, Plugin-System,
Playlist-Engine, Stream-Deck-Integration, Audio-Resilienz-Fixes etc.) siehe
bei Bedarf:

- Projekt: `/home/infantilo/PIPELINE CONTROLLER`
- Doku dort: `/home/infantilo/PIPELINE CONTROLLER/README.md`,
  `/home/infantilo/PIPELINE CONTROLLER/HANDBUCH.html`
- Falls relevante Architektur-/Bugfix-Historie gebraucht wird, dort gezielt
  nachlesen (Dateien mit absolutem Pfad lesen) statt zu raten — nicht
  automatisch übernehmen, da Kontext hier anders sein kann.

Änderungen hier wirken sich **nicht** auf PIPELINE CONTROLLER aus und
umgekehrt.

## Projektstatus

Gerade initialisiert (leeres Git-Repo, Branch `main`). Struktur, Stack und
Ziele werden noch festgelegt.
