# nodes/

Ab Phase C ein Rust-Workspace (`omp-node-sdk` + konkrete Media-Nodes wie
`playout`). Bis dahin: `mock/` (Go, ab Schritt A7) als Test-Node ohne
echte Medientechnik. Siehe `ARCHITECTURE.md` §4.1a, §5.
